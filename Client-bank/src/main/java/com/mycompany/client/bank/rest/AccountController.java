package com.mycompany.client.bank.rest;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class AccountController {
         //1. Page account
	//GetAccountInfoAndTransactions (get)
        //ChangeWallet (post)
        //PostTransaction (post)
        //2. Page Open_Account
        // GetBanks (get)
        //CreateAccount (post)
}
